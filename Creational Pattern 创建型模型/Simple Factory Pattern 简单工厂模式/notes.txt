简单工厂模式

用工厂来创建对象

工厂.getProduct（对象）

通过xml配置文件来更换具体产品

适用：
(1) 工厂类负责创建的对象比较少，由于创建的对象较少，不会造成工厂方法中的业务逻辑太过复杂。
(2) 客户端只知道传入工厂类的参数，对于如何创建对象并不关心。

实际上简单工厂模式并没有被列入到23个GoF设计模式之中，但是这个模式是后面两种工厂模式的基础，也是最基础的设计模式。